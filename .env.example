# Copy this file to .env and replace the value with a unique secret.
# Generate one with:
# python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=replace-with-a-new-random-secret-key
